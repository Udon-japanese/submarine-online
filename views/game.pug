extends layout

block content
  .d-md-inline-flex
    //- レスポンシブなデザインに
    canvas#radar
    h3.ms-3.text-nowrap #{displayName}
      img(src=`${thumbUrl}` alt=`${displayName}のアイコン` width="40" height="40")
    canvas#score.justify-content-md-center
  script#main(src="/javascripts/index/bundle.js" data-displayName=`${displayName}` data-thumbUrl=`${thumbUrl}` data-ipAddress=`${ipAddress}`)
  script(src="/javascripts/key-down-handler.js")